[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "traceagent-verification"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]
