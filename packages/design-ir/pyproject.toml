[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "traceagent-design-ir"
version = "0.1.0"
dependencies = ["pydantic==2.11.3"]

[tool.setuptools.packages.find]
where = ["src"]
